```text id="q7m2nx"
目前项目中的 AI 识别逻辑存在严重问题，需要重新设计。

现在的软件默认认为：
拼豆图纸会直接写“颜色 + 数量”。

但真实拼豆图纸并不是这样。

真实拼豆图纸通常采用：

“颜色编码 + 数量”

例如：

A1 42
A11 10
B10 710
H7 630

其中：

- A1、B10、H7 是拼豆颜色编码
- 后面的数字才是使用数量

请重新修改整个识别与库存系统。

# 一、核心修改目标（非常重要）

系统不能再直接识别“红色、黑色”这种中文颜色。

而是：

先识别：
“颜色编码”

再通过：
本地颜色映射表

转换为真正颜色。

---

# 二、新的业务逻辑

正确流程应该是：

图片上传
→ AI识别编码与数量
→ 提取：

[
  {"code":"A1","count":42},
  {"code":"B10","count":710}
]

→ 查询本地颜色映射表

例如：

A1 -> 奶白色
B10 -> 深黑色

→ 转换为真实库存颜色

→ 扣减库存

---

# 三、必须新增颜色映射系统（重点）

新增：

color_mapping.csv

字段：

code,color_name,alias,brand

示例：

A1,奶白色,米白,Artkal
B10,深黑色,纯黑,Artkal
H7,深灰色,炭灰,Artkal

系统所有库存逻辑：
必须基于 code。

不能再基于中文颜色名称。

---

# 四、库存系统必须重构（重点）

原来的：

inventory.csv

字段：

color,current_stock

需要改成：

code,color_name,current_stock,warning_threshold,update_time

例如：

A1,奶白色,5000,300,2026-05-24
B10,深黑色,12000,500,2026-05-24

库存扣减：

必须根据 code 进行。

因为：

不同品牌可能存在同名颜色。

code 才是真正唯一标识。

---

# 五、AI识别逻辑必须修改（重点）

之前的 Prompt：

错误。

因为 AI 不应该识别中文颜色。

而应该：

专门识别：

“颜色编码 + 数量”

例如：

A1 42
B10 710
H7 630

---

# 六、重新设计 DeepSeek Prompt（重点）

请重新编写发送给 DeepSeek 的 Prompt。

要求：

# AI任务

从拼豆图纸图片中：

提取所有：

- 颜色编码
- 对应数量

---

# 识别规则（非常重要）

颜色编码格式：

通常为：

- 一个字母 + 数字
或
- 两个字母 + 数字

例如：

A1
A11
B10
H7
C25

后面紧跟数量。

例如：

A1 42
B10 710

---

# AI返回格式

必须强制返回：

JSON数组。

例如：

[
  {
    "code":"A1",
    "count":42
  },
  {
    "code":"B10",
    "count":710
  }
]

禁止返回 Markdown。

禁止解释。

禁止自然语言。

只能返回 JSON。

---

# 七、必须增加 OCR预处理（重点）

很多拼豆图纸：

文字很小。

因此：

在发送给 AI 之前：

请增加 OpenCV 预处理：

- 放大图片
- 锐化
- 灰度化
- 对比度增强
- 降噪

提高识别准确率。

---

# 八、新增“识别校验逻辑”（重点）

AI识别后：

系统必须自动校验：

## 1. code是否合法

例如：

必须匹配：

^[A-Z]{1,2}[0-9]+$

---

## 2. count是否合法

必须为正整数。

---

## 3. code是否存在映射表

如果不存在：

弹窗：

“发现未知颜色编码”

允许用户：

- 手动新增颜色映射
- 或忽略

---

# 九、识别结果确认页必须升级

新增：

## 颜色映射展示

例如：

A1 → 奶白色 → 42

B10 → 深黑色 → 710

---

# 十、CSV结构修改（重点）

重新设计：

## inventory.csv

字段：

code,color_name,current_stock,warning_threshold,update_time

---

## color_mapping.csv

字段：

code,color_name,alias,brand

---

## inventory_logs.csv

字段：

type,code,color_name,change,source,time

---

# 十一、UI需要修改

库存页面：

必须显示：

- 编码
- 颜色名称
- 当前库存
- 阈值

而不是只显示颜色。

---

# 十二、增加颜色映射管理页面

新增页面：

“颜色编码管理”

支持：

- 新增编码
- 修改编码
- 搜索编码
- 导入CSV
- 导出CSV

---

# 十三、必须重构代码架构（重点）

请检查当前项目。

重点修改：

- AI识别模块
- CSV结构
- 库存扣减逻辑
- UI表格字段
- 数据模型

避免：

仍然使用旧的“中文颜色识别逻辑”。

---

# 十四、请直接输出：

1. 需要修改的模块
2. 修改后的数据结构
3. 新版 DeepSeek Prompt
4. OpenCV预处理方案
5. 新版库存逻辑
6. 关键代码修改

请直接开始重构方案。
```
